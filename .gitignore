__pycache__/
*.py[cod]

# Virtual envs
venv/
qwen_env/
.qwen_env/

# Checkpoints and training artifacts (HUGE)
**/optimizer.pt
**/scheduler.pt
**/rng_state.pth
**/trainer_state.json
**/training_args.bin

# HuggingFace tokenizer cache copies
**/tokenizer.json
**/tokenizer_config.json
**/special_tokens_map.json
**/vocab.json
**/merges.txt

# UNLESS inside a tokenizer folder:
# For now, we skip them globally to prevent massive uploads.

# Jupyter
.ipynb_checkpoints/
*.ipynb~

# Logs
*.log
logs/

# Python build
*.egg-info/
# Large training data and artifacts (local only)
data/elite_kaggle_lines_clean_top_tier_bars.csv
elite_ngrams.tsv
elite_w2v.model
elite_tokenizer/
lora_adapter_elite/
lora_elite/
lora_elite_v2/
rhyme_siamese/
# ============================
# Project: rap-bot
# ML / NLP Large Files Exclusion
# ============================

# --- MODEL CHECKPOINTS ---
# This repo can optionally track checkpoints metadata/reads.
# Large weight files are still ignored by patterns below (*.pt, *.bin, *.safetensors, etc).
models/
*/adapter_model.safetensors
*.safetensors
*.bin
*.pt
*.pth

# --- TOKENIZERS / VOCAB / Merges (Local Artifacts) ---
elite_tokenizer/
tokenizer.json
added_tokens.json
merges.txt
vocab.json
special_tokens_map.json
tokenizer_config.json

# --- LORA ADAPTERS (Large & Non-versioned) ---
lora_adapter_elite/
lora_elite/
lora_elite_v2/
lora_elite_v2b/

# --- SIAMESE RHYME MODEL (Large Checkpoints) ---
rhyme_siamese/

# --- TRAINING ARTIFACTS ---
optimizer.pt
scheduler.pt
training_args.bin
trainer_state.json
rng_state.pth

# --- LARGE EMBEDDINGS / N-GRAM MODELS ---
elite_w2v.model
elite_ngrams.tsv

# --- RAW / UNPROCESSED DATA (Local Only) ---
data_kaggle/
artifacts/
artifacts/*
*.pickle
*.pkl

# --- LARGE CSVs (Avoid GitHub 100MB limit) ---
artifacts/*.csv
data/*top_tier*.csv

# --- TEMP / CACHE / JUNK FILES ---
*.log
*.tmp
*.cache
__pycache__

# Environment / secrets
.env

# Iterative test artifacts (regenerated on run)
data/iterative_tests/

# Evolution run artifacts (archived to DB; keep repo clean)
data/evo_rhyme/runs/
results_qd*.json
results*.json
*weights*.json
test_output.json
smoke_qd_runtime.json

# --- Local test/cache artifacts (always ignore) ---
.coverage
.pytest_cache/
